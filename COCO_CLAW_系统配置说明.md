# COCO CLAW 系统配置说明

> 更新时间: 2026-04-08
> 说明：本文档区分系统中"写死"的部分和"可配置"的部分

---

## 一、项目技术栈概览

### 1.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                        COCO CLAW                            │
│                      AI技能商城系统                           │
├─────────────┬─────────────┬─────────────┬──────────────────┤
│ coco-claw   │  coco-claw  │  coco-claw  │    MySQL +       │
│   -web      │   -admin    │   -api      │    Redis +       │
│  (C端前端)   │  (管理后台)   │   (后端API)   │    Nacos        │
└─────────────┴─────────────┴─────────────┴──────────────────┘
```

### 1.2 各项目技术栈详情

#### C端前端 (coco-claw-web)
| 类别 | 技术 | 说明 |
|------|------|------|
| 框架 | Nuxt.js 3 | Vue 3 服务端渲染框架 |
| 构建工具 | Vite | 快速的开发服务器 |
| CSS框架 | TailwindCSS | 原子化CSS，响应式设计 |
| 移动端UI | Vant 4 | 轻量级移动端组件库 |
| 状态管理 | Pinia | Vue 3 状态管理 |
| HTTP客户端 | Axios | API请求 |
| 部署 | Vercel / Nginx | 静态部署 |

**仓库地址**: https://github.com/lixuanran457-droid/coco-claw-web

#### 管理后台 (coco-claw-admin)
| 类别 | 技术 | 说明 |
|------|------|------|
| 框架 | React 18 | UI框架 |
| 构建工具 | Vite | 快速的开发服务器 |
| UI组件库 | Ant Design 5 | 企业级React组件库 |
| 路由 | React Router 6 | 页面路由管理 |
| 状态管理 | Zustand | 轻量级状态管理 |
| HTTP客户端 | Axios | API请求 |
| 部署 | Nginx | 静态部署 |

**仓库地址**: https://github.com/lixuanran457-droid/coco-claw-admin

#### 后端API (coco-claw-api)
| 类别 | 技术 | 说明 |
|------|------|------|
| 框架 | Spring Boot 3 | Java后端框架 |
| ORM | MyBatis-Plus | 轻量级ORM框架 |
| 数据库 | MySQL 8 | 关系型数据库 |
| 缓存 | Redis | 高性能缓存 |
| 注册中心 | Nacos | 服务发现与配置 |
| 安全 | JWT | 无状态身份认证 |
| 构建工具 | Maven | 项目构建 |
| 部署 | Docker / 云服务器 | 容器化部署 |

**仓库地址**: https://github.com/lixuanran457-droid/coco-claw-api

### 1.3 第三方服务

| 服务 | 用途 | 状态 |
|------|------|------|
| Nacos | 配置中心 | 已部署 (124.243.191.159:8848) |
| 支付宝/微信 | 支付接口 | 待同事接入 |
| 短信服务 | 验证码/通知 | 待接入 |

---

## 二、已通过管理后台配置的功能 ✅

以下内容可以通过管理后台随时修改，无需改代码：

### 2.1 首页Banner配置
| 配置项 | 后台位置 | 说明 |
|--------|----------|------|
| Banner图片 | Banner管理 → 新增 | 上传Banner图片 |
| Banner链接 | Banner管理 → 编辑 | 点击跳转地址 |
| Banner排序 | Banner管理 → 编辑 | 控制显示顺序 |
| 启用/禁用 | Banner管理 → 操作 | 控制是否显示 |

**后端实现**:
- 数据库表: `sys_banner`
- Controller: `BannerController.java`
- API接口: `GET /api/config/banner`

### 2.2 热门推荐配置
| 配置项 | 后台位置 | 说明 |
|--------|----------|------|
| 推荐技能 | 热门推荐 → 新增 | 选择要推荐的技能 |
| 推荐排序 | 热门推荐 → 编辑 | 控制推荐顺序 |
| 推荐标签 | 热门推荐 → 编辑 | 如"限时特惠"等 |

**后端实现**:
- 数据库表: `recommend_skill`
- Controller: `RecommendController.java`
- API接口: `GET /api/recommend/list`

### 2.3 系统配置
| 配置项 | 后台位置 | 说明 |
|--------|----------|------|
| 货币符号 | 系统配置 → 基本设置 | ¥ / $ / € 等 |
| 货币符号位置 | 系统配置 → 基本设置 | 前面 / 后面 |
| 分页大小 | 系统配置 → 分页设置 | 默认每页数量 |
| Token有效期 | 系统配置 → 安全设置 | JWT过期时间 |

**后端实现**:
- 数据库表: `sys_config`
- Controller: `ConfigController.java`
- API接口: `GET /api/config/currency`

---

## 三、管理后台可配置的部分

### 3.1 技能商品管理

| 配置项 | 后台位置 | 说明 |
|--------|----------|------|
| 上架/下架技能 | 技能管理 → 列表 | 控制用户是否能看到 |
| 技能名称 | 技能管理 → 编辑 | 技能标题 |
| 技能价格 | 技能管理 → 编辑 | 售价设置 |
| 技能分类 | 技能管理 → 编辑 | 所属分类 |
| 技能描述 | 技能管理 → 编辑 | 详细介绍 |
| 技能图标 | 技能管理 → 编辑 | 展示图标 |
| 库存数量 | 技能管理 → 编辑 | 可销售数量 |
| 排序权重 | 技能管理 → 编辑 | 影响展示顺序 |

### 3.2 分类管理

| 配置项 | 后台位置 | 说明 |
|--------|----------|------|
| 添加分类 | 分类管理 → 新增 | 创建新分类 |
| 分类名称 | 分类管理 → 编辑 | 分类标题 |
| 分类图标 | 分类管理 → 编辑 | 展示图标 |
| 父级分类 | 分类管理 → 编辑 | 二级分类设置 |
| 排序 | 分类管理 → 编辑 | 分类显示顺序 |
| 启用/禁用 | 分类管理 → 操作 | 控制分类是否显示 |

### 3.3 订单管理

| 配置项 | 后台位置 | 说明 |
|--------|----------|------|
| 查看订单 | 订单管理 → 列表 | 查看所有订单 |
| 订单状态 | 订单管理 → 详情 | 待支付/已支付/已完成等 |
| 发货处理 | 订单管理 → 详情 | （数字商品可自动） |
| 退款处理 | 订单管理 → 详情 | 用户申请退款时处理 |

### 3.4 用户管理

| 配置项 | 后台位置 | 说明 |
|--------|----------|------|
| 查看用户 | 用户管理 → 列表 | 查看所有注册用户 |
| 封禁用户 | 用户管理 → 操作 | 禁止登录 |
| 解封用户 | 用户管理 → 操作 | 恢复登录 |
| 调整余额 | 用户管理 → 余额调整 | 增加/扣除余额 |
| 发送消息 | 用户管理 → 发送消息 | 站内通知 |

### 3.5 优惠券管理

| 配置项 | 后台位置 | 说明 |
|--------|----------|------|
| 创建优惠券 | 优惠券 → 新增 | 设置优惠规则 |
| 优惠券类型 | 优惠券 → 新增 | 满减/折扣/无门槛 |
| 优惠金额 | 优惠券 → 新增 | 满100减20等 |
| 发放对象 | 优惠券 → 发放 | 指定用户/全部用户 |
| 有效期 | 优惠券 → 新增 | 开始/结束时间 |
| 发放数量 | 优惠券 → 新增 | 限制数量 |

---

## 四、写死代码的部分（需修改源码）

以下内容是直接在代码中写死的，如果要修改需要改代码：

### 4.1 C端前端 (coco-claw-web)

| 功能 | 位置 | 写死内容 | 修改方式 |
|------|------|----------|----------|
| 底部导航 | components/Footer.vue | 导航项和图标 | 改源码 |
| 页面标题 | 各页面 | 固定标题文字 | 改源码 |
| 登录注册页样式 | pages/login.vue | 页面布局 | 改源码 |

### 4.2 后端 (coco-claw-api)

| 配置项 | 位置 | 写死内容 | 修改方式 |
|--------|------|----------|----------|
| 数据库连接 | application.yml | localhost:3306 | 改配置文件 |
| Redis地址 | application.yml | localhost:6379 | 改配置文件 |
| Nacos地址 | application.yml | http://124.243.191.159:8848 | 改配置文件 |
| JWT密钥 | application.yml | 固定密钥字符串 | 改配置文件 |

### 4.3 管理后台 (coco-claw-admin)

| 功能 | 位置 | 写死内容 | 修改方式 |
|------|------|----------|----------|
| 登录页Logo | Login.tsx | Logo图片 | 改源码 |
| 侧边栏菜单 | Sidebar.tsx | 菜单项 | 改源码 |
| 首页统计卡片 | Dashboard.tsx | 统计指标 | 改源码 |

---

## 五、可配置 vs 写死 对比表

| 功能模块 | 可配置部分 | 写死部分 |
|----------|-----------|----------|
| **首页Banner** | ✅ 全部可配置 | 无 |
| **热门推荐** | ✅ 全部可配置 | 无 |
| **系统配置** | ✅ 货币符号/分页等 | 无 |
| 技能商品 | 名称/价格/上下架/分类 | 无 |
| 分类管理 | 全部可配置 | 无 |
| 用户管理 | 封禁/余额/消息 | 登录方式 |
| 优惠券 | 全部可配置 | 无 |
| 订单管理 | 状态处理 | 无 |
| 页面样式 | 颜色/字体大小 | 布局结构 |

---

## 六、数据库表结构

### 6.1 核心业务表

| 表名 | 说明 | 主要字段 |
|------|------|----------|
| `user` | 用户表 | id, username, password, balance |
| `skill` | 技能商品表 | id, name, price, category_id, status |
| `category` | 分类表 | id, name, icon, parent_id |
| `order` | 订单表 | id, user_id, skill_id, status, amount |
| `coupon` | 优惠券表 | id, type, value, min_amount |

### 6.2 配置管理表

| 表名 | 说明 | 主要字段 |
|------|------|----------|
| `sys_config` | 系统配置表 | id, config_key, config_value |
| `sys_banner` | Banner表 | id, image_url, link_url, sort |
| `recommend_skill` | 推荐商品表 | id, skill_id, sort, label |
| `sys_admin` | 管理员表 | id, username, password, role |

### 6.3 关联关系

```
user (1) ──── (N) order
user (1) ──── (N) coupon_usage
category (1) ──── (N) skill
skill (N) ──── (M) recommend_skill
```

---

## 七、API接口一览

### 7.1 用户端接口 (C端前端调用)

| 模块 | 接口 | 方法 | 说明 |
|------|------|------|------|
| 认证 | `/api/auth/login` | POST | 用户登录 |
| 认证 | `/api/auth/register` | POST | 用户注册 |
| 认证 | `/api/auth/forgot` | POST | 找回密码 |
| 技能 | `/api/skill/list` | GET | 技能列表 |
| 技能 | `/api/skill/detail/{id}` | GET | 技能详情 |
| 分类 | `/api/category/tree` | GET | 分类树形结构 |
| 订单 | `/api/order/list` | GET | 我的订单 |
| 订单 | `/api/order/create` | POST | 创建订单 |
| 配置 | `/api/config/banner` | GET | 获取Banner |
| 配置 | `/api/config/currency` | GET | 获取货币符号 |
| 推荐 | `/api/recommend/list` | GET | 热门推荐 |

### 7.2 管理端接口 (管理后台调用)

| 模块 | 接口 | 方法 | 说明 |
|------|------|------|------|
| 管理员 | `/api/admin/login` | POST | 管理员登录 |
| 管理员 | `/api/admin/list` | GET | 管理员列表 |
| 管理员 | `/api/admin/add` | POST | 添加管理员 |
| 管理员 | `/api/admin/update` | PUT | 更新管理员 |
| 管理员 | `/api/admin/delete` | DELETE | 删除管理员 |
| 系统配置 | `/api/system/config/*` | CRUD | 系统配置管理 |
| Banner | `/api/system/banner/*` | CRUD | Banner管理 |
| 推荐 | `/api/system/recommend/*` | CRUD | 推荐管理 |

---

## 八、总结

| 类型 | 数量 | 说明 |
|------|------|------|
| **完全可配置** | ~20项 | Banner/推荐/系统配置/技能/分类/用户/订单/优惠券 |
| **部分可配置** | ~3项 | 底部导航/页面标题 |
| **完全写死** | ~6项 | 基础结构，不建议改 |

| 项目 | 技术栈 | 代码量 |
|------|--------|--------|
| coco-claw-web | Nuxt.js 3 + TailwindCSS + Vant + Pinia | ~20 Vue文件 |
| coco-claw-admin | React + Ant Design + Vite + Zustand | ~40 React组件 |
| coco-claw-api | Spring Boot 3 + MyBatis-Plus + MySQL | ~48 Java类 |

**核心业务（首页展示、卖什么、多少钱、优惠活动）都可以通过管理后台配置！** 🎉
